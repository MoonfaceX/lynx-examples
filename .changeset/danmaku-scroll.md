---
"@lynx-example/danmaku-scroll": minor
---

Add a danmaku-scroll example demonstrating an infinite horizontal bullet-comment marquee built with a `<list>` and main thread scripts (A/B dual-segment recycling plus main-thread boundary normalization), using UI-only mock data.
